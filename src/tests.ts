import { expect } from 'chai';
import { asdf } from './index';

describe('First Module', () => {

    it('', () => {});
    
    it('this should work', () => {
        expect(1).to.equal(1);
    });
    
    it('this should be ignored', () => {
        console.log(2);
    });
});
